// Auto-generated wrapper for contract 'FeeSplitterV2'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "@contracts/launcher-v2/fee-splitter-messages"
import "@contracts/launcher-v2/fee-splitter-types"

/// Storage `FeeSplitterStorage` is defined in `@contracts/launcher-v2/fee-splitter-types`
struct FeeSplitterV2 {
    address: address
    stateInit: ContractState? = null
}

/// Creates a contract wrapper instance from the storage data
fun FeeSplitterV2.fromStorage(
    storage: FeeSplitterStorage,
    toShard: AddressShardingOptions? = null,
): FeeSplitterV2 {
    val stateInit = ContractState {
        code: build("FeeSplitterV2"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { stateInit, toShard }.calculateAddress();
    return FeeSplitterV2 { address, stateInit };
}

/// Creates a contract wrapper instance from the address
fun FeeSplitterV2.fromAddress(address: address): FeeSplitterV2 {
    return FeeSplitterV2 { address };
}

/// Deploys the contract to the blockchain
fun FeeSplitterV2.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun FeeSplitterV2.sendFeeIn(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: FeeIn { queryId, amount },
    });
    return net.send(from, genericMsg);
}

fun FeeSplitterV2.sendDistribute(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Distribute { queryId },
    });
    return net.send(from, genericMsg);
}

fun FeeSplitterV2.sendSetSplitterVault(
    self,
    from: address,
    queryId: uint64,
    vault: address,
    active: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetSplitterVault { queryId, vault, active },
    });
    return net.send(from, genericMsg);
}

fun FeeSplitterV2.sendSetSplitterActive(
    self,
    from: address,
    queryId: uint64,
    active: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetSplitterActive { queryId, active },
    });
    return net.send(from, genericMsg);
}

fun FeeSplitterV2.sendProposeSplits(
    self,
    from: address,
    queryId: uint64,
    splits: FeeSplits,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ProposeSplits { queryId, splits },
    });
    return net.send(from, genericMsg);
}

fun FeeSplitterV2.sendApplySplits(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ApplySplits { queryId },
    });
    return net.send(from, genericMsg);
}

fun FeeSplitterV2.sendChangeSplitterAdmin(
    self,
    from: address,
    queryId: uint64,
    newAdmin: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ChangeSplitterAdmin { queryId, newAdmin },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun FeeSplitterV2.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun FeeSplitterV2.getSplitterData(self): FeeSplitterDataReply {
    return net.runGetMethod(self.address, "get_splitter_data");
}

fun FeeSplitterV2.getCodeVersion(self): int {
    return net.runGetMethod(self.address, "get_code_version");
}
