// Auto-generated wrapper for contract 'DeskItem'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "@contracts/common/tep-nft"
import "@contracts/desk/desk-messages"
import "@contracts/desk/desk-types"
import "@contracts/vendor/jetton/messages"

/// Storage `DeskItemStorageNotInitialized` is defined in `@contracts/desk/desk-types`
struct DeskItem {
    address: address
    stateInit: ContractState? = null
}

/// Creates a contract wrapper instance from the storage data
fun DeskItem.fromStorage(
    storage: DeskItemStorageNotInitialized,
    toShard: AddressShardingOptions? = null,
): DeskItem {
    val stateInit = ContractState {
        code: build("DeskItem"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { stateInit, toShard }.calculateAddress();
    return DeskItem { address, stateInit };
}

/// Creates a contract wrapper instance from the address
fun DeskItem.fromAddress(address: address): DeskItem {
    return DeskItem { address };
}

/// Deploys the contract to the blockchain
fun DeskItem.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun DeskItem.sendAskToChangeOwnership(
    self,
    from: address,
    queryId: uint64,
    newOwnerAddress: address,
    sendExcessesTo: address?,
    customPayload: cell?,
    forwardTonAmount: coins,
    forwardPayload: PayloadInline | PayloadInRef,
    config: SendParams = {},
): SendResultList {
    // build body cell manually, because some fields have @abi.clientType
    val bodyB = beginCell()
        .storeUint(0x5fcc3d14, 32)
        .storeAny(queryId)
        .storeAny(newOwnerAddress)
        .storeAny(sendExcessesTo)
        .storeAny(customPayload)
        .storeAny(forwardTonAmount)
        .storeAny(forwardPayload);
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: AskToChangeOwnership.fromSlice(bodyB.toSlice()),
    });
    return net.send(from, genericMsg);
}

fun DeskItem.sendRequestStaticData(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: RequestStaticData { queryId },
    });
    return net.send(from, genericMsg);
}

fun DeskItem.sendStampsInit(
    self,
    from: address,
    queryId: uint64,
    stamps: map<uint8, uint128>,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StampsInit { queryId, stamps },
    });
    return net.send(from, genericMsg);
}

fun DeskItem.sendSweep(
    self,
    from: address,
    queryId: uint64,
    slotsMask: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Sweep { queryId, slotsMask },
    });
    return net.send(from, genericMsg);
}

fun DeskItem.sendSweepAck(
    self,
    from: address,
    queryId: uint64,
    slot: uint8,
    newStamp: uint128,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SweepAck { queryId, slot, newStamp },
    });
    return net.send(from, genericMsg);
}

fun DeskItem.sendWithdraw(
    self,
    from: address,
    queryId: uint64,
    slot: uint8,
    jettonWallet: address,
    amount: coins,
    to: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Withdraw { queryId, slot, jettonWallet, amount, to },
    });
    return net.send(from, genericMsg);
}

fun DeskItem.sendWithdrawOk(
    self,
    from: address,
    queryId: uint64,
    slot: uint8,
    jettonWallet: address,
    amount: coins,
    to: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WithdrawOk { queryId, slot, jettonWallet, amount, to },
    });
    return net.send(from, genericMsg);
}

fun DeskItem.sendTransferNotificationForRecipient(
    self,
    from: address,
    queryId: uint64,
    jettonAmount: coins,
    transferInitiator: address?,
    forwardPayload: PayloadInline | PayloadInRef,
    config: SendParams = {},
): SendResultList {
    // build body cell manually, because some fields have @abi.clientType
    val bodyB = beginCell()
        .storeUint(0x7362d09c, 32)
        .storeAny(queryId)
        .storeAny(jettonAmount)
        .storeAny(transferInitiator)
        .storeAny(forwardPayload);
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TransferNotificationForRecipient.fromSlice(bodyB.toSlice()),
    });
    return net.send(from, genericMsg);
}

fun DeskItem.sendReturnExcessesBack(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ReturnExcessesBack { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun DeskItem.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun DeskItem.getNftData(self): NftDataReply {
    return net.runGetMethod(self.address, "get_nft_data");
}

fun DeskItem.getDeskData(self): DeskDataReply {
    return net.runGetMethod(self.address, "get_desk_data");
}
