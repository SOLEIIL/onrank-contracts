// Auto-generated wrapper for contract 'Pool'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "@contracts/launcher/launcher-messages"
import "@contracts/launcher/launcher-types"
import "@contracts/vendor/jetton/messages"

/// Storage `PoolStorage` is defined in `@contracts/launcher/launcher-types`
struct Pool {
    address: address
    stateInit: ContractState? = null
}

/// Creates a contract wrapper instance from the storage data
fun Pool.fromStorage(storage: PoolStorage, toShard: AddressShardingOptions? = null): Pool {
    val stateInit = ContractState {
        code: build("Pool"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { stateInit, toShard }.calculateAddress();
    return Pool { address, stateInit };
}

/// Creates a contract wrapper instance from the address
fun Pool.fromAddress(address: address): Pool {
    return Pool { address };
}

/// Deploys the contract to the blockchain
fun Pool.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun Pool.sendSwapTonForCoin(
    self,
    from: address,
    queryId: uint64,
    minOut: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SwapTonForCoin { queryId, minOut },
    });
    return net.send(from, genericMsg);
}

fun Pool.sendTransferNotificationForRecipient(
    self,
    from: address,
    queryId: uint64,
    jettonAmount: coins,
    transferInitiator: address?,
    forwardPayload: PayloadInline | PayloadInRef,
    config: SendParams = {},
): SendResultList {
    // build body cell manually, because some fields have @abi.clientType
    val bodyB = beginCell()
        .storeUint(0x7362d09c, 32)
        .storeAny(queryId)
        .storeAny(jettonAmount)
        .storeAny(transferInitiator)
        .storeAny(forwardPayload);
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TransferNotificationForRecipient.fromSlice(bodyB.toSlice()),
    });
    return net.send(from, genericMsg);
}

fun Pool.sendReturnExcessesBack(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ReturnExcessesBack { queryId },
    });
    return net.send(from, genericMsg);
}

fun Pool.sendTopUpTons(self, from: address, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TopUpTons {},
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun Pool.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun Pool.getPoolData(self): PoolDataReply {
    return net.runGetMethod(self.address, "get_pool_data");
}

fun Pool.getBuyQuote(self, tonIn: coins): Quote {
    return net.runGetMethod(self.address, "get_buy_quote", [tonIn]);
}

fun Pool.getSellQuote(self, coinIn: coins): Quote {
    return net.runGetMethod(self.address, "get_sell_quote", [coinIn]);
}

fun Pool.getSwapGas(self): coins {
    return net.runGetMethod(self.address, "get_swap_gas");
}
