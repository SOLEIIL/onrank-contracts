// Auto-generated wrapper for contract 'BetPosition'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "@contracts/predict/predict-messages"
import "@contracts/predict/predict-types"

/// Storage `BetPositionStorage` is defined in `@contracts/predict/predict-types`
struct BetPosition {
    address: address
    stateInit: ContractState? = null
}

/// Creates a contract wrapper instance from the storage data
fun BetPosition.fromStorage(
    storage: BetPositionStorage,
    toShard: AddressShardingOptions? = null,
): BetPosition {
    val stateInit = ContractState {
        code: build("BetPosition"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { stateInit, toShard }.calculateAddress();
    return BetPosition { address, stateInit };
}

/// Creates a contract wrapper instance from the address
fun BetPosition.fromAddress(address: address): BetPosition {
    return BetPosition { address };
}

/// Deploys the contract to the blockchain
fun BetPosition.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun BetPosition.sendRecordBet(
    self,
    from: address,
    queryId: uint64,
    isYes: bool,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: RecordBet { queryId, isYes, amount },
    });
    return net.send(from, genericMsg);
}

fun BetPosition.sendClaim(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Claim { queryId },
    });
    return net.send(from, genericMsg);
}

fun BetPosition.sendClaimAck(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ClaimAck { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun BetPosition.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun BetPosition.getBetPositionData(self): BetPositionDataReply {
    return net.runGetMethod(self.address, "get_bet_position_data");
}
