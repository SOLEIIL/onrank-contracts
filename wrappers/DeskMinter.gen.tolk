// Auto-generated wrapper for contract 'DeskMinter'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "@contracts/desk/desk-messages"
import "@contracts/vendor/jetton/messages"

/// Storage `DeskMinterStorage` is defined in `@contracts/desk/desk-messages`
struct DeskMinter {
    address: address
    stateInit: ContractState? = null
}

/// Creates a contract wrapper instance from the storage data
fun DeskMinter.fromStorage(
    storage: DeskMinterStorage,
    toShard: AddressShardingOptions? = null,
): DeskMinter {
    val stateInit = ContractState {
        code: build("DeskMinter"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { stateInit, toShard }.calculateAddress();
    return DeskMinter { address, stateInit };
}

/// Creates a contract wrapper instance from the address
fun DeskMinter.fromAddress(address: address): DeskMinter {
    return DeskMinter { address };
}

/// Deploys the contract to the blockchain
fun DeskMinter.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun DeskMinter.sendTransferNotificationForRecipient(
    self,
    from: address,
    queryId: uint64,
    jettonAmount: coins,
    transferInitiator: address?,
    forwardPayload: PayloadInline | PayloadInRef,
    config: SendParams = {},
): SendResultList {
    // build body cell manually, because some fields have @abi.clientType
    val bodyB = beginCell()
        .storeUint(0x7362d09c, 32)
        .storeAny(queryId)
        .storeAny(jettonAmount)
        .storeAny(transferInitiator)
        .storeAny(forwardPayload);
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TransferNotificationForRecipient.fromSlice(bodyB.toSlice()),
    });
    return net.send(from, genericMsg);
}

fun DeskMinter.sendDeployDeskOk(
    self,
    from: address,
    queryId: uint64,
    serial: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DeployDeskOk { queryId, serial },
    });
    return net.send(from, genericMsg);
}

fun DeskMinter.sendRetryDeploy(
    self,
    from: address,
    queryId: uint64,
    serial: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: RetryDeploy { queryId, serial },
    });
    return net.send(from, genericMsg);
}

fun DeskMinter.sendRetryBurn(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: RetryBurn { queryId },
    });
    return net.send(from, genericMsg);
}

fun DeskMinter.sendSetDeskMinterPaused(
    self,
    from: address,
    queryId: uint64,
    paused: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetDeskMinterPaused { queryId, paused },
    });
    return net.send(from, genericMsg);
}

fun DeskMinter.sendSetDeskMinterConfig(
    self,
    from: address,
    queryId: uint64,
    config_: DeskMinterConfig,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetDeskMinterConfig {
            queryId,
            config: config_.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun DeskMinter.sendChangeDeskMinterAdmin(
    self,
    from: address,
    queryId: uint64,
    newAdmin: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ChangeDeskMinterAdmin { queryId, newAdmin },
    });
    return net.send(from, genericMsg);
}

fun DeskMinter.sendReturnExcessesBack(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ReturnExcessesBack { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun DeskMinter.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun DeskMinter.getMinterData(self): DeskMinterDataReply {
    return net.runGetMethod(self.address, "get_minter_data");
}
