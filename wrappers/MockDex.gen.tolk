// Auto-generated wrapper for contract 'MockDex'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "@contracts/common/dedust"
import "@contracts/test/MockDex"
import "@contracts/vendor/jetton/messages"

/// Storage `MockDexStorage` is defined in `@contracts/test/MockDex`
struct MockDex {
    address: address
    stateInit: ContractState? = null
}

/// Creates a contract wrapper instance from the storage data
fun MockDex.fromStorage(storage: MockDexStorage, toShard: AddressShardingOptions? = null): MockDex {
    val stateInit = ContractState {
        code: build("MockDex"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { stateInit, toShard }.calculateAddress();
    return MockDex { address, stateInit };
}

/// Creates a contract wrapper instance from the address
fun MockDex.fromAddress(address: address): MockDex {
    return MockDex { address };
}

/// Deploys the contract to the blockchain
fun MockDex.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun MockDex.sendDedustNativeSwap(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    pool: address,
    kind: bool,
    limit: coins,
    next: cell?,
    params: DedustSwapParams,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustNativeSwap {
            queryId,
            amount,
            pool,
            kind,
            limit,
            next,
            params: params.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun MockDex.sendSetMockDex(
    self,
    from: address,
    queryId: uint64,
    jettonWallet: address?,
    rate: coins,
    refuseNext: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetMockDex { queryId, jettonWallet, rate, refuseNext },
    });
    return net.send(from, genericMsg);
}

fun MockDex.sendTransferNotificationForRecipient(
    self,
    from: address,
    queryId: uint64,
    jettonAmount: coins,
    transferInitiator: address?,
    forwardPayload: PayloadInline | PayloadInRef,
    config: SendParams = {},
): SendResultList {
    // build body cell manually, because some fields have @abi.clientType
    val bodyB = beginCell()
        .storeUint(0x7362d09c, 32)
        .storeAny(queryId)
        .storeAny(jettonAmount)
        .storeAny(transferInitiator)
        .storeAny(forwardPayload);
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TransferNotificationForRecipient.fromSlice(bodyB.toSlice()),
    });
    return net.send(from, genericMsg);
}

fun MockDex.sendReturnExcessesBack(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ReturnExcessesBack { queryId },
    });
    return net.send(from, genericMsg);
}

fun MockDex.sendTopUpTons(self, from: address, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TopUpTons {},
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun MockDex.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun MockDex.estimateSwapOut(self, assetIn: slice, amountIn: coins): MockEstimate {
    return net.runGetMethod(self.address, "estimate_swap_out", [assetIn, amountIn]);
}

fun MockDex.getMockData(self): MockDexStorage {
    return net.runGetMethod(self.address, "get_mock_data");
}
