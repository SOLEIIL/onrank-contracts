// Auto-generated wrapper for contract 'RewardWalletV2'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "@contracts/launcher-v2/reward-messages"
import "@contracts/launcher-v2/reward-types"
import "@contracts/vendor/jetton/messages"
import "@contracts/vendor/jetton/storage"

/// Storage `RewardWalletStorage` is defined in `@contracts/launcher-v2/reward-types`
struct RewardWalletV2 {
    address: address
    stateInit: ContractState? = null
}

/// Creates a contract wrapper instance from the storage data
fun RewardWalletV2.fromStorage(
    storage: RewardWalletStorage,
    toShard: AddressShardingOptions? = null,
): RewardWalletV2 {
    val stateInit = ContractState {
        code: build("RewardWalletV2"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { stateInit, toShard }.calculateAddress();
    return RewardWalletV2 { address, stateInit };
}

/// Creates a contract wrapper instance from the address
fun RewardWalletV2.fromAddress(address: address): RewardWalletV2 {
    return RewardWalletV2 { address };
}

/// Deploys the contract to the blockchain
fun RewardWalletV2.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun RewardWalletV2.sendAskToTransfer(
    self,
    from: address,
    queryId: uint64,
    jettonAmount: coins,
    transferRecipient: address,
    sendExcessesTo: address?,
    customPayload: cell?,
    forwardTonAmount: coins,
    forwardPayload: PayloadInline | PayloadInRef,
    config: SendParams = {},
): SendResultList {
    // build body cell manually, because some fields have @abi.clientType
    val bodyB = beginCell()
        .storeUint(0x0f8a7ea5, 32)
        .storeAny(queryId)
        .storeAny(jettonAmount)
        .storeAny(transferRecipient)
        .storeAny(sendExcessesTo)
        .storeAny(customPayload)
        .storeAny(forwardTonAmount)
        .storeAny(forwardPayload);
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: AskToTransfer.fromSlice(bodyB.toSlice()),
    });
    return net.send(from, genericMsg);
}

fun RewardWalletV2.sendAskToBurn(
    self,
    from: address,
    queryId: uint64,
    jettonAmount: coins,
    sendExcessesTo: address?,
    customPayload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: AskToBurn { queryId, jettonAmount, sendExcessesTo, customPayload },
    });
    return net.send(from, genericMsg);
}

fun RewardWalletV2.sendClaimRewards(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ClaimRewards { queryId },
    });
    return net.send(from, genericMsg);
}

fun RewardWalletV2.sendCancelSettle(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CancelSettle { queryId },
    });
    return net.send(from, genericMsg);
}

fun RewardWalletV2.sendRewardTransferStep(
    self,
    from: address,
    queryId: uint64,
    jettonAmount: coins,
    transferInitiator: address?,
    sendExcessesTo: address?,
    forwardTonAmount: coins,
    indices: map<uint8, uint128>,
    forwardPayload: PayloadInline | PayloadInRef,
    config: SendParams = {},
): SendResultList {
    // build body cell manually, because some fields have @abi.clientType
    val bodyB = beginCell()
        .storeUint(0x52570003, 32)
        .storeAny(queryId)
        .storeAny(jettonAmount)
        .storeAny(transferInitiator)
        .storeAny(sendExcessesTo)
        .storeAny(forwardTonAmount)
        .storeAny(indices)
        .storeAny(forwardPayload);
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: RewardTransferStep.fromSlice(bodyB.toSlice()),
    });
    return net.send(from, genericMsg);
}

fun RewardWalletV2.sendPoolDeliver(
    self,
    from: address,
    queryId: uint64,
    jettonAmount: coins,
    sendExcessesTo: address,
    forwardTonAmount: coins,
    indices: map<uint8, uint128>,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: PoolDeliver { queryId, jettonAmount, sendExcessesTo, forwardTonAmount, indices },
    });
    return net.send(from, genericMsg);
}

fun RewardWalletV2.sendSettleAck(
    self,
    from: address,
    queryId: uint64,
    retry: bool,
    vault: address?,
    indices: map<uint8, uint128>,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SettleAck { queryId, retry, vault, indices },
    });
    return net.send(from, genericMsg);
}

fun RewardWalletV2.sendRestoreAccrued(
    self,
    from: address,
    queryId: uint64,
    rewardId: uint8,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: RestoreAccrued { queryId, rewardId, amount },
    });
    return net.send(from, genericMsg);
}

fun RewardWalletV2.sendTopUpTons(self, from: address, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TopUpTons {},
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun RewardWalletV2.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun RewardWalletV2.getWalletData(self): JettonWalletDataReply {
    return net.runGetMethod(self.address, "get_wallet_data");
}

fun RewardWalletV2.getRewardWalletData(self): RewardWalletDataReply {
    return net.runGetMethod(self.address, "get_reward_wallet_data");
}

fun RewardWalletV2.getClaimable(self, indices: map<uint8, uint128>): map<uint8, coins> {
    return net.runGetMethod(self.address, "get_claimable", [indices]);
}

fun RewardWalletV2.getCodeVersion(self): int {
    return net.runGetMethod(self.address, "get_code_version");
}
