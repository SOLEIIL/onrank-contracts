// Auto-generated wrapper for contract 'PotRelay'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "@contracts/pot/relay-messages"
import "@contracts/vendor/jetton/messages"

/// Storage `PotRelayStorage` is defined in `@contracts/pot/relay-messages`
struct PotRelay {
    address: address
    stateInit: ContractState? = null
}

/// Creates a contract wrapper instance from the storage data
fun PotRelay.fromStorage(
    storage: PotRelayStorage,
    toShard: AddressShardingOptions? = null,
): PotRelay {
    val stateInit = ContractState {
        code: build("PotRelay"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { stateInit, toShard }.calculateAddress();
    return PotRelay { address, stateInit };
}

/// Creates a contract wrapper instance from the address
fun PotRelay.fromAddress(address: address): PotRelay {
    return PotRelay { address };
}

/// Deploys the contract to the blockchain
fun PotRelay.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun PotRelay.sendTransferNotificationForRecipient(
    self,
    from: address,
    queryId: uint64,
    jettonAmount: coins,
    transferInitiator: address?,
    forwardPayload: PayloadInline | PayloadInRef,
    config: SendParams = {},
): SendResultList {
    // build body cell manually, because some fields have @abi.clientType
    val bodyB = beginCell()
        .storeUint(0x7362d09c, 32)
        .storeAny(queryId)
        .storeAny(jettonAmount)
        .storeAny(transferInitiator)
        .storeAny(forwardPayload);
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TransferNotificationForRecipient.fromSlice(bodyB.toSlice()),
    });
    return net.send(from, genericMsg);
}

fun PotRelay.sendFlush(
    self,
    from: address,
    queryId: uint64,
    jettonWallet: address,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Flush { queryId, jettonWallet, amount },
    });
    return net.send(from, genericMsg);
}

fun PotRelay.sendSetRelayRoles(
    self,
    from: address,
    queryId: uint64,
    admin: address,
    keeper: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetRelayRoles { queryId, admin, keeper },
    });
    return net.send(from, genericMsg);
}

fun PotRelay.sendSetUsdtWallet(
    self,
    from: address,
    queryId: uint64,
    usdtWallet: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetUsdtWallet { queryId, usdtWallet },
    });
    return net.send(from, genericMsg);
}

fun PotRelay.sendReturnExcessesBack(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ReturnExcessesBack { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun PotRelay.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun PotRelay.getRelayData(self): RelayDataReply {
    return net.runGetMethod(self.address, "get_relay_data");
}
