// Auto-generated wrapper for contract 'BuybackVault'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "@contracts/launcher/buyback-messages"
import "@contracts/launcher/buyback-types"
import "@contracts/launcher/fee-splitter-messages"
import "@contracts/launcher/reward-messages"
import "@contracts/vendor/jetton/messages"

/// Storage `BuybackStorage` is defined in `@contracts/launcher/buyback-types`
struct BuybackVault {
    address: address
    stateInit: ContractState? = null
}

/// Creates a contract wrapper instance from the storage data
fun BuybackVault.fromStorage(
    storage: BuybackStorage,
    toShard: AddressShardingOptions? = null,
): BuybackVault {
    val stateInit = ContractState {
        code: build("BuybackVault"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { stateInit, toShard }.calculateAddress();
    return BuybackVault { address, stateInit };
}

/// Creates a contract wrapper instance from the address
fun BuybackVault.fromAddress(address: address): BuybackVault {
    return BuybackVault { address };
}

/// Deploys the contract to the blockchain
fun BuybackVault.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun BuybackVault.sendBuybackIn(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: BuybackIn { queryId },
    });
    return net.send(from, genericMsg);
}

fun BuybackVault.sendBuybackBuy(
    self,
    from: address,
    queryId: uint64,
    tonAmount: coins,
    minOut: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: BuybackBuy { queryId, tonAmount, minOut },
    });
    return net.send(from, genericMsg);
}

fun BuybackVault.sendBuybackBurn(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: BuybackBurn { queryId, amount },
    });
    return net.send(from, genericMsg);
}

fun BuybackVault.sendCancelBuyback(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CancelBuyback { queryId },
    });
    return net.send(from, genericMsg);
}

fun BuybackVault.sendTransferNotificationForRecipient(
    self,
    from: address,
    queryId: uint64,
    jettonAmount: coins,
    transferInitiator: address?,
    forwardPayload: PayloadInline | PayloadInRef,
    config: SendParams = {},
): SendResultList {
    // build body cell manually, because some fields have @abi.clientType
    val bodyB = beginCell()
        .storeUint(0x7362d09c, 32)
        .storeAny(queryId)
        .storeAny(jettonAmount)
        .storeAny(transferInitiator)
        .storeAny(forwardPayload);
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TransferNotificationForRecipient.fromSlice(bodyB.toSlice()),
    });
    return net.send(from, genericMsg);
}

fun BuybackVault.sendResponseWalletAddress(
    self,
    from: address,
    queryId: uint64,
    jettonWalletAddress: address?,
    ownerAddress: Cell<address>?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ResponseWalletAddress { queryId, jettonWalletAddress, ownerAddress },
    });
    return net.send(from, genericMsg);
}

fun BuybackVault.sendSyncBuybackMarket(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SyncBuybackMarket { queryId },
    });
    return net.send(from, genericMsg);
}

fun BuybackVault.sendMarketAnswer(
    self,
    from: address,
    queryId: uint64,
    curveOpen: bool,
    curve: address?,
    pool: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MarketAnswer { queryId, curveOpen, curve, pool },
    });
    return net.send(from, genericMsg);
}

fun BuybackVault.sendSetBuybackLimits(
    self,
    from: address,
    queryId: uint64,
    reserve: coins,
    maxPerBuy: coins,
    pendingTimeout: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetBuybackLimits { queryId, reserve, maxPerBuy, pendingTimeout },
    });
    return net.send(from, genericMsg);
}

fun BuybackVault.sendSetBuybackRoles(
    self,
    from: address,
    queryId: uint64,
    admin: address,
    keeper: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetBuybackRoles { queryId, admin, keeper },
    });
    return net.send(from, genericMsg);
}

fun BuybackVault.sendTopUpTons(self, from: address, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TopUpTons {},
    });
    return net.send(from, genericMsg);
}

fun BuybackVault.sendReturnExcessesBack(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ReturnExcessesBack { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun BuybackVault.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun BuybackVault.getBuybackData(self): BuybackDataReply {
    return net.runGetMethod(self.address, "get_buyback_data");
}
