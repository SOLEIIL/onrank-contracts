// Auto-generated wrapper for contract 'Market'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "@contracts/predict/predict-messages"
import "@contracts/predict/predict-types"

/// Storage `MarketStorage` is defined in `@contracts/predict/predict-types`
struct Market {
    address: address
    stateInit: ContractState? = null
}

/// Creates a contract wrapper instance from the storage data
fun Market.fromStorage(storage: MarketStorage, toShard: AddressShardingOptions? = null): Market {
    val stateInit = ContractState {
        code: build("Market"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { stateInit, toShard }.calculateAddress();
    return Market { address, stateInit };
}

/// Creates a contract wrapper instance from the address
fun Market.fromAddress(address: address): Market {
    return Market { address };
}

/// Deploys the contract to the blockchain
fun Market.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun Market.sendPlaceBet(
    self,
    from: address,
    queryId: uint64,
    isYes: bool,
    partner: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: PlaceBet { queryId, isYes, partner },
    });
    return net.send(from, genericMsg);
}

fun Market.sendLockOpen(
    self,
    from: address,
    queryId: uint64,
    price: uint128,
    publishTime: uint32,
    attestation: uint256,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: LockOpen { queryId, price, publishTime, attestation },
    });
    return net.send(from, genericMsg);
}

fun Market.sendResolve(
    self,
    from: address,
    queryId: uint64,
    price: uint128,
    publishTime: uint32,
    attestation: uint256,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Resolve { queryId, price, publishTime, attestation },
    });
    return net.send(from, genericMsg);
}

fun Market.sendForceVoid(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ForceVoid { queryId },
    });
    return net.send(from, genericMsg);
}

fun Market.sendClaimPayout(
    self,
    from: address,
    queryId: uint64,
    owner: address,
    yes: coins,
    no: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ClaimPayout { queryId, owner, yes, no },
    });
    return net.send(from, genericMsg);
}

fun Market.sendSweepHouse(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SweepHouse { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun Market.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun Market.getMarketData(self): MarketDataReply {
    return net.runGetMethod(self.address, "get_market_data");
}

fun Market.getMarketState(self): MarketStateReply {
    return net.runGetMethod(self.address, "get_market_state");
}

fun Market.getPositionAddress(self, owner: address): address {
    return net.runGetMethod(self.address, "get_position_address", [owner]);
}
