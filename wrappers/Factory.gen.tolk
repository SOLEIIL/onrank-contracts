// Auto-generated wrapper for contract 'Factory'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "@contracts/launcher/factory-messages"
import "@contracts/launcher/factory-types"
import "@contracts/launcher/fee-splitter-types"
import "@contracts/vendor/jetton/messages"

/// Storage `FactoryStorage` is defined in `@contracts/launcher/factory-types`
struct Factory {
    address: address
    stateInit: ContractState? = null
}

/// Creates a contract wrapper instance from the storage data
fun Factory.fromStorage(storage: FactoryStorage, toShard: AddressShardingOptions? = null): Factory {
    val stateInit = ContractState {
        code: build("Factory"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { stateInit, toShard }.calculateAddress();
    return Factory { address, stateInit };
}

/// Creates a contract wrapper instance from the address
fun Factory.fromAddress(address: address): Factory {
    return Factory { address };
}

/// Deploys the contract to the blockchain
fun Factory.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun Factory.sendCreateLaunch(
    self,
    from: address,
    queryId: uint64,
    metadata: cell,
    rewards: map<uint8, address>,
    firstBuyTon: coins,
    splits: FeeSplits?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CreateLaunch { queryId, metadata, rewards, firstBuyTon, splits },
    });
    return net.send(from, genericMsg);
}

fun Factory.sendSetFactoryPaused(
    self,
    from: address,
    queryId: uint64,
    paused: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetFactoryPaused { queryId, paused },
    });
    return net.send(from, genericMsg);
}

fun Factory.sendSetFactoryConfig(
    self,
    from: address,
    queryId: uint64,
    config_: FactoryConfig,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetFactoryConfig {
            queryId,
            config: config_.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun Factory.sendSetFactoryRoles(
    self,
    from: address,
    queryId: uint64,
    roles: FactoryRoles,
    routes: FactoryRoutes,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetFactoryRoles {
            queryId,
            roles: roles.toCell(),
            routes: routes.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun Factory.sendTopUpTons(self, from: address, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TopUpTons {},
    });
    return net.send(from, genericMsg);
}

fun Factory.sendReturnExcessesBack(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ReturnExcessesBack { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun Factory.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun Factory.getFactoryData(self): FactoryDataReply {
    return net.runGetMethod(self.address, "get_factory_data");
}

fun Factory.getLaunchAddresses(
    self,
    seq: uint64,
    metadata: cell,
    rewards: map<uint8, address>,
    splits: Cell<FeeSplits>?,
): LaunchAddresses {
    return net.runGetMethod(self.address, "get_launch_addresses", [seq, metadata, rewards, splits]);
}
