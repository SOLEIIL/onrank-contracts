// Auto-generated wrapper for contract 'PoolV2'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "@contracts/launcher-v2/launcher-messages"
import "@contracts/launcher-v2/launcher-types"
import "@contracts/launcher-v2/reward-messages"
import "@contracts/vendor/jetton/messages"

/// Storage `PoolStorage` is defined in `@contracts/launcher-v2/launcher-types`
struct PoolV2 {
    address: address
    stateInit: ContractState? = null
}

/// Creates a contract wrapper instance from the storage data
fun PoolV2.fromStorage(storage: PoolStorage, toShard: AddressShardingOptions? = null): PoolV2 {
    val stateInit = ContractState {
        code: build("PoolV2"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { stateInit, toShard }.calculateAddress();
    return PoolV2 { address, stateInit };
}

/// Creates a contract wrapper instance from the address
fun PoolV2.fromAddress(address: address): PoolV2 {
    return PoolV2 { address };
}

/// Deploys the contract to the blockchain
fun PoolV2.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun PoolV2.sendSwapTonForCoin(
    self,
    from: address,
    queryId: uint64,
    minOut: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SwapTonForCoin { queryId, minOut },
    });
    return net.send(from, genericMsg);
}

fun PoolV2.sendSellNotify(
    self,
    from: address,
    queryId: uint64,
    owner: address,
    amount: coins,
    minTon: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SellNotify { queryId, owner, amount, minTon },
    });
    return net.send(from, genericMsg);
}

fun PoolV2.sendPoolOutFailed(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: PoolOutFailed { queryId, amount },
    });
    return net.send(from, genericMsg);
}

fun PoolV2.sendTransferNotificationForRecipient(
    self,
    from: address,
    queryId: uint64,
    jettonAmount: coins,
    transferInitiator: address?,
    forwardPayload: PayloadInline | PayloadInRef,
    config: SendParams = {},
): SendResultList {
    // build body cell manually, because some fields have @abi.clientType
    val bodyB = beginCell()
        .storeUint(0x7362d09c, 32)
        .storeAny(queryId)
        .storeAny(jettonAmount)
        .storeAny(transferInitiator)
        .storeAny(forwardPayload);
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TransferNotificationForRecipient.fromSlice(bodyB.toSlice()),
    });
    return net.send(from, genericMsg);
}

fun PoolV2.sendReturnExcessesBack(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ReturnExcessesBack { queryId },
    });
    return net.send(from, genericMsg);
}

fun PoolV2.sendTopUpTons(self, from: address, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TopUpTons {},
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun PoolV2.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun PoolV2.getPoolData(self): PoolDataReply {
    return net.runGetMethod(self.address, "get_pool_data");
}

fun PoolV2.getBuyQuote(self, tonIn: coins): Quote {
    return net.runGetMethod(self.address, "get_buy_quote", [tonIn]);
}

fun PoolV2.getSellQuote(self, coinIn: coins): Quote {
    return net.runGetMethod(self.address, "get_sell_quote", [coinIn]);
}

fun PoolV2.getSwapGas(self): coins {
    return net.runGetMethod(self.address, "get_swap_gas");
}

fun PoolV2.getCodeVersion(self): int {
    return net.runGetMethod(self.address, "get_code_version");
}
