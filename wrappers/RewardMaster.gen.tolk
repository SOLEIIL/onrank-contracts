// Auto-generated wrapper for contract 'RewardMaster'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "@contracts/launcher/reward-messages"
import "@contracts/launcher/reward-types"
import "@contracts/vendor/jetton/messages"
import "@contracts/vendor/jetton/storage"

/// Storage `RewardMasterStorage` is defined in `@contracts/launcher/reward-types`
struct RewardMaster {
    address: address
    stateInit: ContractState? = null
}

/// Creates a contract wrapper instance from the storage data
fun RewardMaster.fromStorage(
    storage: RewardMasterStorage,
    toShard: AddressShardingOptions? = null,
): RewardMaster {
    val stateInit = ContractState {
        code: build("RewardMaster"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { stateInit, toShard }.calculateAddress();
    return RewardMaster { address, stateInit };
}

/// Creates a contract wrapper instance from the address
fun RewardMaster.fromAddress(address: address): RewardMaster {
    return RewardMaster { address };
}

/// Deploys the contract to the blockchain
fun RewardMaster.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun RewardMaster.sendSettle(
    self,
    from: address,
    queryId: uint64,
    owner: address,
    balance: coins,
    action: uint8,
    burnAmount: coins,
    sellMinTon: coins?,
    stamps: map<uint8, uint128>,
    accrued: map<uint8, coins>,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Settle { queryId, owner, balance, action, burnAmount, sellMinTon, stamps, accrued },
    });
    return net.send(from, genericMsg);
}

fun RewardMaster.sendMintReward(
    self,
    from: address,
    queryId: uint64,
    to: address,
    amount: coins,
    forwardTonAmount: coins,
    forwardPayload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MintReward { queryId, to, amount, forwardTonAmount, forwardPayload },
    });
    return net.send(from, genericMsg);
}

fun RewardMaster.sendIndexBump(
    self,
    from: address,
    queryId: uint64,
    rewardId: uint8,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: IndexBump { queryId, rewardId, amount },
    });
    return net.send(from, genericMsg);
}

fun RewardMaster.sendExcludedUpdate(
    self,
    from: address,
    queryId: uint64,
    balance: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ExcludedUpdate { queryId, balance },
    });
    return net.send(from, genericMsg);
}

fun RewardMaster.sendCurveGraduated(
    self,
    from: address,
    queryId: uint64,
    pool: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CurveGraduated { queryId, pool },
    });
    return net.send(from, genericMsg);
}

fun RewardMaster.sendWireMaster(
    self,
    from: address,
    queryId: uint64,
    curve: address,
    vault: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WireMaster { queryId, curve, vault },
    });
    return net.send(from, genericMsg);
}

fun RewardMaster.sendRequestWalletAddress(
    self,
    from: address,
    queryId: uint64,
    ownerAddress: address,
    includeOwnerAddress: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: RequestWalletAddress { queryId, ownerAddress, includeOwnerAddress },
    });
    return net.send(from, genericMsg);
}

fun RewardMaster.sendQueryMarket(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: QueryMarket { queryId },
    });
    return net.send(from, genericMsg);
}

fun RewardMaster.sendTopUpTons(self, from: address, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TopUpTons {},
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun RewardMaster.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun RewardMaster.getJettonData(self): JettonDataReply {
    return net.runGetMethod(self.address, "get_jetton_data");
}

fun RewardMaster.getWalletAddress(self, ownerAddress: address): address {
    return net.runGetMethod(self.address, "get_wallet_address", [ownerAddress]);
}

fun RewardMaster.getRewardMasterData(self): RewardMasterDataReply {
    return net.runGetMethod(self.address, "get_reward_master_data");
}
